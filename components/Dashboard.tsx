import Link from "next/link"; import {ArrowRight,FileText,Mail,Upload} from "lucide-react"; import {stats} from "../data/mockData";
export default function Dashboard(){return <div className="page"><div className="hero"><div><span className="kicker">WORKFLOW OVERVIEW</span><h2>Automate documents from data to inbox.</h2><p>Upload your spreadsheet, personalize documents, generate PDFs, send emails and monitor every delivery from one workspace.</p><Link className="primary-btn" href="/upload">Start New Workflow <ArrowRight size={17}/></Link></div><div className="hero-visual"><Node icon={<Upload/>} text="Upload data"/><div className="connector"/><Node icon={<FileText/>} text="Generate PDF"/><div className="connector"/><Node icon={<Mail/>} text="Send email"/></div></div><div className="stats-grid"><Stat label="Documents generated" value={stats.documents} icon="DOC"/><Stat label="Emails processed" value={stats.emails} icon="MAIL"/><Stat label="Success rate" value={`${stats.successRate}%`} icon="OK"/></div><div className="section-head"><h3>How the workflow works</h3><p>Each stage is isolated so backend services can be added without rewriting the UI.</p></div><div className="cards-grid"><Card n="01" title="Upload & validate" text="Import CSV or Excel data and validate required fields." href="/upload"/><Card n="02" title="Generate documents" text="Choose a template and create personalized documents." href="/generate"/><Card n="03" title="Email & track" text="Configure delivery and monitor sent, pending or failed jobs." href="/email"/></div></div>}
function Node({icon,text}){return <div className="mini-node">{icon}<span>{text}</span></div>}function Stat({label,value,icon}){return <div className="stat-card"><div className="stat-icon">{icon}</div><div><strong>{value}</strong><span>{label}</span></div></div>}function Card({n,title,text,href}){return <Link href={href} className="feature-card"><span>{n}</span><h4>{title}</h4><p>{text}</p><ArrowRight size={18}/></Link>}
