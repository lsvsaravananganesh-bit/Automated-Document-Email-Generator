import { CheckCircle2, AlertCircle } from "lucide-react";
export default function DataTable({records=[],errors=[]}){const headers=records.length?Object.keys(records[0]):[];return <div>{errors.length>0&&<div className="alert"><AlertCircle size={18}/><div>{errors.slice(0,5).map(e=><div key={e}>{e}</div>)}</div></div>}<div className="table-wrap"><table><thead><tr>{headers.map(h=><th key={h}>{h.replaceAll("_"," ")}</th>)}</tr></thead><tbody>{records.map((row,i)=><tr key={i}>{headers.map(h=><td key={h}>{String(row[h]??"")}</td>)}</tr>)}</tbody></table></div>{records.length>0&&errors.length===0&&<div className="valid-message"><CheckCircle2 size={17}/> All visible records passed validation.</div>}</div>}
