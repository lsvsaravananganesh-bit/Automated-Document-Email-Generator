import StatusBadge from "../../components/StatusBadge"; import {trackingData} from "../../data/trackingData"; import ProgressBar from "../../components/ProgressBar";
export default function TrackingPage(){const delivered=trackingData.filter(x=>x.status==="Delivered"||x.status==="Sent").length;return <div className="page"><div className="page-intro"><span className="kicker">STEP 05</span><h2>Tracking & history</h2><p>Monitor document and email jobs. Supabase will become the source of truth when the backend is connected.</p></div><div className="tracking-top"><div className="panel"><strong>{delivered}/{trackingData.length}</strong><span>Successful demo jobs</span><ProgressBar value={Math.round(delivered/trackingData.length*100)} label="Delivery progress"/></div><div className="panel"><strong>{trackingData.length}</strong><span>Total recent jobs</span><p className="muted">Sent, pending and failed statuses are represented below.</p></div></div><div className="panel"><div className="table-heading"><div><h3>Recent activity</h3><p>Latest document and email jobs</p></div></div><div className="table-wrap"><table><thead><tr><th>Job ID</th><th>Recipient</th><th>Document</th><th>Status</th><th>Date</th></tr></thead><tbody>{trackingData.map(x=><tr key={x.id}><td><strong>{x.id}</strong></td><td>{x.recipient}</td><td>{x.document}</td><td><StatusBadge status={x.status}/></td><td>{x.date}</td></tr>)}</tbody></table></div></div></div>}
